// Ejercicio 1:
/* Escribe una función que tome un número como argumento y devuelva su cuadrado.
   La función debe devolver el número multiplicado por sí mismo. */
export function cuadrado(numero) {
    // tu código aquí
}

// Ejercicio 2:
/* Escribe una función que tome dos números como argumentos y devuelva el mayor 
   de los dos. Si ambos números son iguales, puede devolver cualquiera de los dos. */
export function mayorDeDos(num1, num2) {
    // tu código aquí
}

// Ejercicio 3:
/* Escribe una función que tome una cadena y devuelva la misma cadena en 
   mayúsculas. Puedes usar el método toUpperCase() de JavaScript. */
export function convertirMayusculas(cadena) {
    // tu código aquí
}

// Ejercicio 4:
/* Escribe una función que tome un array de números y devuelva la suma de todos
   los números en el array. Usa un bucle para iterar sobre el array. */
export function sumarArray(numeros) {
    // tu código aquí
}

// Ejercicio 5:
/* Escribe una función que tome un array de palabras y devuelva la palabra más
   larga del array. Si hay varias palabras con la misma longitud, devuelve la 
   primera que encuentres. */
export function palabraMasLarga(palabras) {
    // tu código aquí
}

// Ejercicio 6:
/* Escribe una función que tome un número como argumento y devuelva "par" si el
   número es par y "impar" si el número es impar. Puedes usar el operador de
   módulo (%) para determinar la paridad. */
export function parOImpar(numero) {
    // tu código aquí
}

// Ejercicio 7:
/* Escribe una función que tome una cadena y devuelva la misma cadena con la
   primera letra de cada palabra en mayúsculas. Puedes usar el método split() 
   para separar las palabras y join() para unirlas de nuevo. */
export function capitalizarPrimeraLetra(cadena) {
    // tu código aquí
}

// Ejercicio 8:
/* Escribe una función que tome un array de números y devuelva el número más
   pequeño del array. Usa un bucle para comparar los números o el método 
   Math.min() con el operador de propagación (...). */
export function numeroMasPequeno(numeros) {
    // tu código aquí
}

// Ejercicio 9:
/* Escribe una función que tome un array de números y devuelva un nuevo array con
   solo los números pares. Usa un bucle para filtrar los números o el método
   filter() de los arrays. */
export function filtrarPares(numeros) {
    // tu código aquí
}

// Ejercicio 10:
/* Escribe una función que tome una cadena y devuelva la misma cadena pero al 
   revés. Usa un bucle para invertir la cadena o el método split(), reverse() y
   join() de los arrays. */
export function invertirCadena(cadena) {
    // tu código aquí
}

// Ejercicio 11:
/* Escribe una función que tome un array de números y devuelva el promedio de 
   esos números. Usa un bucle para sumar los números y luego divide por la 
   cantidad de números en el array. */
export function promedioArray(numeros) {
    // tu código aquí
}

// Ejercicio 12:
/* Escribe una función que tome una cadena y devuelva la cantidad de vocales que
   hay en la cadena. Puedes usar un bucle para contar las vocales o el método 
   match() con una expresión regular. */
export function contarVocales(cadena) {
    // tu código aquí
}

// Ejercicio 13:
/* Escribe una función que tome dos cadenas y devuelva true si la segunda cadena
   es una subcadena de la primera, y false en caso contrario. Usa el método 
   includes() de las cadenas. */
export function esSubcadena(cadena, subcadena) {
    // tu código aquí
}

// Ejercicio 14:
/* Escribe una función que tome un array de números y devuelva un nuevo array con
   los números en orden ascendente. Usa el método sort() de los arrays. */
export function ordenarAscendente(numeros) {
    // tu código aquí
}

// Ejercicio 15:
/* Escribe una función que tome un array de números y devuelva un nuevo array con
   los números en orden descendente. Usa el método sort() con una función de 
   comparación. */
export function ordenarDescendente(numeros) {
    // tu código aquí
}

// Ejercicio 16:
/* Escribe una función que tome una cadena y devuelva la misma cadena con todas
   las vocales reemplazadas por el carácter '*'. Usa una expresión regular con 
   el método replace() de las cadenas. */
export function reemplazarVocales(cadena) {
    // tu código aquí
}

// Ejercicio 17:
/* Escribe una función que tome un array de objetos, donde cada objeto tiene una 
   propiedad 'edad', y devuelva el promedio de las edades. Usa un bucle para 
   sumar las edades y luego divide por la cantidad de objetos en el array. */
export function promedioEdades(personas) {
    // tu código aquí
}

// Ejercicio 18:
/* Escribe una función que tome una cadena y devuelva true si la cadena es un
   palíndromo (se lee igual de adelante hacia atrás y de atrás hacia adelante),
   y false en caso contrario. */
export function esPalindromo(cadena) {
    // tu código aquí
}

// Ejercicio 19:
/* Escribe una función que tome un array de números y devuelva un nuevo array 
   con los números elevados al cuadrado. Usa el método map() de los arrays. */
export function elevarAlCuadrado(numeros) {
    // tu código aquí
}

// Ejercicio 20:
/* Escribe una función que tome un array de números y devuelva la suma de los
   números pares. Usa un bucle para sumar los números o el método filter() y
   reduce() de los arrays. */
export function sumaPares(numeros) {
    // tu código aquí
}
